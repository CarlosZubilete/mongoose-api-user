# MONGOOSE API USER - Environment Configuration Template
# 
# This file provides a template for environment variables required by the application.
# Copy this file to .env and populate with your actual configuration values.
#
# SECURITY WARNING: Never commit the .env file to version control!
# Store sensitive credentials in secure vaults for production environments.

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# MongoDB connection string with authentication credentials
# 
# IMPORTANT: Choose the connection string based on your deployment method:
#
# OPTION 1: Docker Compose Deployment (RECOMMENDED for containerized setup)
# ─────────────────────────────────────────────────────────────────────────
# Inside Docker containers, use the service name 'mongodb' as hostname.
# This enables automatic DNS resolution within the Docker network.
# Username and password must match values in ./mongodb/db/mongodb_root_username.txt
# and ./mongodb/db/mongodb_root_password.txt
#
MONGODB_URL_STRING="mongodb://api-user_user:apiuserpassword@mongodb:27017/api-user_db?authSource=admin&directConnection=true"
#
# OPTION 2: Local Development (without Docker)
# ─────────────────────────────────────────────────────────────────────────
# Use localhost with port 27030 (mapped from MongoDB container's 27017)
# Only works if you're running containers and accessing from host machine.
#
# MONGODB_URL_STRING="mongodb://api-user_user:apiuserpassword@127.0.0.1:27030/api-user_db?authSource=admin"
#
# OPTION 3: MongoDB Atlas (Cloud)
# ─────────────────────────────────────────────────────────────────────────
# For production or cloud-based MongoDB:
# 1. Visit MongoDB Atlas Dashboard: https://cloud.mongodb.com
# 2. Cluster → Connect → Drivers → Copy connection string
# 3. Replace <username>, <password>, and <database> with actual values
#
# MONGODB_URL_STRING="mongodb+srv://username:password@cluster.mongodb.net/database-name?appName=api-users"
#
# ─────────────────────────────────────────────────────────────────────────
# Default: Using Docker Compose (Option 1)
# Uncomment your preferred option above and comment out others
# ─────────────────────────────────────────────────────────────────────────

# ============================================================================
# AUTHENTICATION CONFIGURATION
# ============================================================================
# JWT Secret Key for token signing and verification
# 
# Security Guidelines:
# - Use a strong, random string of at least 30 characters
# - Recommended: Use bcrypt-hashed secret for additional security
# - Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - Change regularly in production environments
# - Never use the same secret in development and production
#
# Example (pre-hashed with bcryptjs):
JWT_SECRET="your-very-strong-jwt-secret-key-here"

# Bcrypt salt rounds for password hashing
# 
# Recommendations:
# - Development: 10-12 rounds (faster, acceptable for dev)
# - Production: 12-14 rounds (stronger security, slower processing)
# - Higher values = more secure but slower login process
# - Each increment roughly doubles computation time
#
SALT_ROUND=12

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Node.js environment setting
# 
# Allowed values:
# - "development"   : Development environment with verbose logging
# - "production"    : Production environment with optimizations
# - "test"          : Testing environment (if test suite is configured)
#
NODE_ENV="development"

# Server port for API listening
# 
# Considerations:
# - Development: Any available port (1024-65535)
# - Production: Typically 80 (HTTP) or 443 (HTTPS) via reverse proxy
# - Common choices: 3000, 4000, 5000, 8080
# - Ensure firewall rules allow the selected port
#
PORT=4000

